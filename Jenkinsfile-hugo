pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo  'hello le monde'
            }
        }
        stage('Doc') {
            steps {
                mkdir(dir:"./doc/")
                sh  'cat *.md > doc/README.md'
            }
        }
    }
}

